"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function CreateClientButton(){const [open,setOpen]=useState(false),[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");const fd=new FormData(e.currentTarget);const r=await fetch("/api/clients",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:fd.get("name"),legalName:fd.get("legalName"),inn:fd.get("inn")})});if(!r.ok){const j=await r.json().catch(()=>({}));setError(j.error??"Ошибка сохранения");return}setOpen(false);router.refresh()}return <><button className="button primary" onClick={()=>setOpen(true)}>+ Клиент</button>{open&&<><div className="drawer-backdrop" onClick={()=>setOpen(false)}/><aside className="drawer"><button className="icon-button drawer-close" onClick={()=>setOpen(false)}>×</button><span className="eyebrow">Create</span><h2>Новый клиент</h2><form className="login-form" onSubmit={submit} style={{marginTop:20}}><label style={{gridColumn:"1/-1"}}>Название<input name="name" required/></label><label style={{gridColumn:"1/-1"}}>Юридическое название<input name="legalName"/></label><label style={{gridColumn:"1/-1"}}>ИНН<input name="inn"/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary">Создать</button></form></aside></>}</>}
