"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { KeyValue, Section, Status } from "@/components/UI";
import type { CandidateProfile, RecruitingApplicationRow } from "@/lib/recruiting/service";
import { contactChannelLabels, recruitingStageLabels, recruitingStages, type RecruitingStage } from "@/lib/recruiting/model";

type Props={profile:CandidateProfile|null;candidateId:string;demo:boolean;canEdit:boolean;canConvert:boolean};
type Tab="overview"|"applications"|"communications"|"history";
const appStorage="operis.recruiting.applications.v1";
const profileStorage="operis.recruiting.profiles.v1";
const commStorage="operis.recruiting.communications.v1";

export function CandidateProfileWorkspace({profile,candidateId,demo,canEdit,canConvert}:Props){
 const router=useRouter();const [tab,setTab]=useState<Tab>("overview");const [current,setCurrent]=useState<CandidateProfile|null>(profile);const [editing,setEditing]=useState(false);const [busy,setBusy]=useState("");const [error,setError]=useState("");const [comm,setComm]=useState({applicationId:"",channel:"phone",direction:"outbound",summary:""});
 useEffect(()=>{if(!demo)return;try{const custom=JSON.parse(localStorage.getItem(profileStorage)||"{}")[candidateId] as Partial<CandidateProfile>|undefined;const apps=JSON.parse(localStorage.getItem(appStorage)||"[]") as RecruitingApplicationRow[];const candidateApps=apps.filter(x=>x.candidateId===candidateId);if(current||candidateApps.length){const first=current?.applications[0]??candidateApps[0];const localComms=JSON.parse(localStorage.getItem(commStorage)||"[]").filter((x:{candidateId:string})=>x.candidateId===candidateId);setCurrent(prev=>({id:candidateId,fullName:custom?.fullName??prev?.fullName??first.fullName,phone:custom?.phone??prev?.phone??first.phone,email:custom?.email??prev?.email??first.email,preferredChannel:custom?.preferredChannel??prev?.preferredChannel??first.preferredChannel,telegram:custom?.telegram??prev?.telegram??first.telegram,whatsapp:custom?.whatsapp??prev?.whatsapp??first.whatsapp,city:custom?.city??prev?.city??first.city,birthDate:custom?.birthDate??prev?.birthDate??null,source:custom?.source??prev?.source??first.source,sourceChannel:custom?.sourceChannel??prev?.sourceChannel??first.sourceChannel,sourceCampaign:custom?.sourceCampaign??prev?.sourceCampaign??first.sourceCampaign,sourceReference:custom?.sourceReference??prev?.sourceReference??first.sourceReference,notes:custom?.notes??prev?.notes??null,status:prev?.status??"active",applications:[...candidateApps,...(prev?.applications??[]).filter(x=>!candidateApps.some(y=>y.applicationId===x.applicationId))],communications:[...localComms,...(prev?.communications??[])],history:prev?.history??[]}));}}catch{}},[candidateId,demo]);
 const latest=current?.applications[0]??null;const tabs:[Tab,string][]=[["overview","Обзор"],["applications","Заявки"],["communications","Коммуникации"],["history","История"]];
 async function saveProfile(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(!current)return;setBusy("profile");setError("");const data=new FormData(event.currentTarget);const payload={fullName:String(data.get("fullName")||""),phone:value(data,"phone"),email:value(data,"email"),preferredChannel:value(data,"preferredChannel"),telegram:value(data,"telegram"),whatsapp:value(data,"whatsapp"),city:value(data,"city"),birthDate:value(data,"birthDate"),source:value(data,"source"),sourceChannel:value(data,"sourceChannel"),sourceCampaign:value(data,"sourceCampaign"),sourceReference:value(data,"sourceReference"),notes:value(data,"notes")};try{if(demo){const all=JSON.parse(localStorage.getItem(profileStorage)||"{}");all[candidateId]=payload;localStorage.setItem(profileStorage,JSON.stringify(all));setCurrent(x=>x?{...x,...payload}:x);}else{const response=await fetch(`/api/candidates/${candidateId}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const json=await response.json().catch(()=>({}));if(!response.ok)throw new Error(json.error??"Не удалось сохранить карточку");router.refresh();}setEditing(false);}catch(e){setError(e instanceof Error?e.message:"Не удалось сохранить карточку");}finally{setBusy("")}}
 async function changeStage(application:RecruitingApplicationRow,stage:RecruitingStage){if(stage===application.stage)return;let reason:string|undefined;if(["rejected","no_show"].includes(stage)){reason=window.prompt(stage==="rejected"?"Причина отказа":"Почему кандидат не вышел")?.trim()||undefined;if(!reason)return;}if(stage==="started"&&!canConvert&&!demo){setError("Недостаточно прав для фиксации фактического выхода");return;}setBusy(application.applicationId);setError("");try{if(demo){const apps=JSON.parse(localStorage.getItem(appStorage)||"[]") as RecruitingApplicationRow[];const next=apps.map(x=>x.applicationId===application.applicationId?{...x,stage,stageLabel:recruitingStageLabels[stage],rejectionReason:reason??null,actualStartAt:stage==="started"?new Date().toISOString():x.actualStartAt}:x);localStorage.setItem(appStorage,JSON.stringify(next));setCurrent(x=>x?{...x,applications:x.applications.map(a=>a.applicationId===application.applicationId?{...a,stage,stageLabel:recruitingStageLabels[stage],rejectionReason:reason??null}:a)}:x);}else{const response=await fetch(`/api/candidates/${candidateId}/stage`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({applicationId:application.applicationId,stage,reason})});const json=await response.json().catch(()=>({}));if(!response.ok)throw new Error(json.error??"Не удалось изменить этап");router.refresh();}}catch(e){setError(e instanceof Error?e.message:"Не удалось изменить этап");}finally{setBusy("")}}
 async function addCommunication(event:React.FormEvent){event.preventDefault();if(!current||!comm.summary.trim())return;setBusy("communication");setError("");try{if(demo){const row={id:crypto.randomUUID(),candidateId,applicationId:comm.applicationId||null,channel:comm.channel,direction:comm.direction,summary:comm.summary,happenedAt:new Date().toLocaleString("ru-RU"),author:"Текущий пользователь"};const all=JSON.parse(localStorage.getItem(commStorage)||"[]");all.unshift(row);localStorage.setItem(commStorage,JSON.stringify(all));setCurrent(x=>x?{...x,communications:[row,...x.communications]}:x);}else{const response=await fetch(`/api/candidates/${candidateId}/communications`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({applicationId:comm.applicationId||null,channel:comm.channel,direction:comm.direction,summary:comm.summary})});const json=await response.json().catch(()=>({}));if(!response.ok)throw new Error(json.error??"Не удалось добавить коммуникацию");router.refresh();}setComm(x=>({...x,summary:""}));}catch(e){setError(e instanceof Error?e.message:"Не удалось добавить коммуникацию");}finally{setBusy("")}}
 if(!current)return <div className="empty"><strong>Кандидат не найден</strong><span>Карточка недоступна в вашем контуре.</span><Link className="button" href="/candidates">Вернуться к кандидатам</Link></div>;
 return <div className="recruiting-workspace">
  {error&&<div className="recruiting-error">{error}</div>}
  <nav className="entity-tabs" aria-label="Разделы карточки кандидата">{tabs.map(([key,label])=><button key={key} className={tab===key?"active":""} onClick={()=>setTab(key)}>{label}{key==="applications"&&<span>{current.applications.length}</span>}{key==="communications"&&<span>{current.communications.length}</span>}</button>)}</nav>
  {tab==="overview"&&<div className="candidate-profile-grid"><div>{editing?<Section title="Редактирование карточки" note="Единые данные человека для всех его заявок"><form className="recruiting-form" onSubmit={saveProfile}><div className="recruiting-form-grid"><Field name="fullName" label="ФИО" value={current.fullName} required/><Field name="phone" label="Телефон" value={current.phone}/><Field name="email" label="Email" value={current.email} type="email"/><label>Предпочтительный канал<select name="preferredChannel" defaultValue={current.preferredChannel??"phone"}><option value="phone">Телефон</option><option value="whatsapp">WhatsApp</option><option value="telegram">Telegram</option><option value="email">Email</option><option value="other">Другой</option></select></label><Field name="telegram" label="Telegram" value={current.telegram}/><Field name="whatsapp" label="WhatsApp" value={current.whatsapp}/><Field name="city" label="Город" value={current.city}/><Field name="birthDate" label="Дата рождения" value={current.birthDate} type="date"/><Field name="source" label="Источник" value={current.source}/><Field name="sourceChannel" label="Канал / площадка" value={current.sourceChannel}/><Field name="sourceCampaign" label="Кампания / объявление" value={current.sourceCampaign}/><Field name="sourceReference" label="Ссылка / ID источника" value={current.sourceReference}/><label className="wide">Комментарий<textarea name="notes" defaultValue={current.notes??""}/></label></div><div className="recruiting-form-actions"><button type="button" className="button" onClick={()=>setEditing(false)}>Отмена</button><button className="button primary" disabled={busy==="profile"}>Сохранить</button></div></form></Section>:<Section title="Основные данные" note="Одна карточка человека независимо от количества объектов" actions={canEdit||demo?<button className="button" onClick={()=>setEditing(true)}>Редактировать</button>:undefined}><div className="candidate-identity"><KeyValue label="Телефон" value={current.phone??"—"}/><KeyValue label="Email" value={current.email??"—"}/><KeyValue label="Предпочтительный канал" value={contactChannelLabels[current.preferredChannel??""]??"—"}/><KeyValue label="Telegram" value={current.telegram??"—"}/><KeyValue label="WhatsApp" value={current.whatsapp??"—"}/><KeyValue label="Город" value={current.city??"—"}/><KeyValue label="Дата рождения" value={current.birthDate??"—"}/><KeyValue label="Статус" value={current.status==="worker"?"Переведён в сотрудники":"Кандидат"}/></div></Section>}<Section title="Атрибуция" note="Для аналитики источников и рекламных кампаний"><div className="candidate-identity"><KeyValue label="Источник" value={current.source??"—"}/><KeyValue label="Канал" value={current.sourceChannel??"—"}/><KeyValue label="Кампания / объявление" value={current.sourceCampaign??"—"}/><KeyValue label="Ссылка / ID" value={current.sourceReference??"—"}/></div></Section></div><div><Section title="Текущий статус"><div style={{padding:"6px 15px 14px"}}>{latest?<><KeyValue label="Потребность" value={latest.need}/><KeyValue label="Объект" value={latest.object??"—"}/><KeyValue label="Этап" value={<Status tone={latest.stage==="started"?"good":"info"}>{latest.stageLabel}</Status>}/><KeyValue label="Рекрутер" value={latest.owner??"—"}/><KeyValue label="Менеджер / мастер" value={latest.manager??"—"}/><KeyValue label="Следующее действие" value={latest.nextAction??"Не запланировано"}/></>:<span>Нет активных заявок</span>}</div></Section><Section title="Комментарий"><div style={{padding:"6px 15px 14px",fontSize:12,lineHeight:1.5}}>{current.notes??"Комментарий не добавлен"}</div></Section></div></div>}
  {tab==="applications"&&<Section title="Заявки кандидата" note="Один человек может участвовать в нескольких потребностях, история не теряется"><div className="candidate-applications">{current.applications.map(application=><div className="candidate-application-row" key={application.applicationId}><div><strong>{application.need}</strong><span>{application.object??"Без объекта"}</span></div><div><Status tone={application.stage==="started"?"good":["rejected","no_show"].includes(application.stage)?"bad":"info"}>{application.stageLabel}</Status><span>{application.rejectionReason??""}</span></div><div><strong>{application.owner??"Без рекрутера"}</strong><span>{application.nextAction??"Нет действия"}</span></div><div><select value={application.stage} disabled={busy===application.applicationId||(!canEdit&&!demo)} onChange={e=>void changeStage(application,e.target.value as RecruitingStage)}>{recruitingStages.map(stage=><option key={stage} value={stage}>{recruitingStageLabels[stage]}</option>)}<option value="rejected">Отказ</option><option value="no_show">Не вышел</option></select></div></div>)}</div></Section>}
  {tab==="communications"&&<div className="candidate-profile-grid"><Section title="Хронология коммуникаций" note="Звонки, мессенджеры, встречи и внутренние заметки"><div className="candidate-history">{current.communications.length?current.communications.map(row=><div className="candidate-history-item" key={row.id}><time>{row.happenedAt}</time><div><strong>{row.author} · {row.channel}</strong><p>{row.summary}</p></div></div>):<div className="empty-inline">Коммуникаций пока нет</div>}</div></Section><Section title="Добавить запись"><form className="recruiting-form" onSubmit={addCommunication}><label>Заявка<select value={comm.applicationId} onChange={e=>setComm(x=>({...x,applicationId:e.target.value}))}><option value="">Общая по кандидату</option>{current.applications.map(x=><option key={x.applicationId} value={x.applicationId}>{x.need} · {x.object??"без объекта"}</option>)}</select></label><label>Канал<select value={comm.channel} onChange={e=>setComm(x=>({...x,channel:e.target.value}))}><option value="phone">Телефон</option><option value="whatsapp">WhatsApp</option><option value="telegram">Telegram</option><option value="email">Email</option><option value="meeting">Встреча</option><option value="note">Заметка</option></select></label><label>Направление<select value={comm.direction} onChange={e=>setComm(x=>({...x,direction:e.target.value}))}><option value="outbound">Исходящая</option><option value="inbound">Входящая</option><option value="internal">Внутренняя</option></select></label><label>Результат / заметка<textarea required value={comm.summary} onChange={e=>setComm(x=>({...x,summary:e.target.value}))}/></label><button className="button primary" disabled={busy==="communication"||(!canEdit&&!demo)}>Добавить запись</button></form></Section></div>}
  {tab==="history"&&<Section title="Системная история этапов" note="Отдельно от человеческих комментариев и коммуникаций"><div className="candidate-history">{current.history.length?current.history.map(row=><div className="candidate-history-item" key={row.id}><time>{row.changedAt}</time><div><strong>{row.changedBy}: {stageLabel(row.fromStage)} → {stageLabel(row.toStage)}</strong>{row.reason&&<p>{row.reason}</p>}</div></div>):<div className="empty-inline">Изменений этапов пока нет</div>}</div></Section>}
 </div>
}
function Field({name,label,value,type="text",required=false}:{name:string;label:string;value:string|null;type?:string;required?:boolean}){return <label>{label}<input name={name} type={type} defaultValue={value??""} required={required}/></label>}
function value(data:FormData,key:string){const result=String(data.get(key)??"").trim();return result||null}
function stageLabel(value:string|null){if(!value)return "Создан";return recruitingStageLabels[value as RecruitingStage]??(value==="call"?"Контакт":value==="documents"?"Подготовка":value==="first_shift"?"Вышел":value)}
