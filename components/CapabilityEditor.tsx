"use client";
import { useEffect, useState } from "react";
import { ShieldCheck } from "lucide-react";

type Grant={capability:string;description:string;domain:string;effect:"inherit"|"allow"|"deny";scopeType:string|null;scopeIds:string[]};
export function CapabilityEditor({targetType,targetId,canManage}:{targetType:"position"|"process_role";targetId:string;canManage:boolean}){const [items,setItems]=useState<Grant[]|null>(null);const [error,setError]=useState("");const [saving,setSaving]=useState("");useEffect(()=>{let active=true;fetch(`/api/organization/grants?targetType=${targetType}&targetId=${targetId}`).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error);if(active)setItems(body.items)}).catch(value=>active&&setError(value.message));return()=>{active=false}},[targetId,targetType]);async function update(item:Grant,effect:Grant["effect"]){setSaving(item.capability);setError("");const response=await fetch("/api/organization/grants",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({targetType,targetId,capability:item.capability,effect,scopeType:item.scopeType??"all_org",scopeIds:item.scopeIds})});const body=await response.json();setSaving("");if(!response.ok){setError(body.error);return}setItems(value=>value?.map(entry=>entry.capability===item.capability?{...entry,effect}:entry)??null)}if(error)return <p className="form-message">{error}</p>;if(items===null)return <p>Загрузка capability-набора…</p>;if(!items.length)return <p>Доступные capabilities не найдены.</p>;return <div className="capability-editor">{items.map(item=><div key={item.capability}><ShieldCheck size={14}/><span><strong>{item.description||item.capability}</strong><small>{item.capability} · {item.scopeType??"область наследуется"}</small></span><select aria-label={`Правило ${item.capability}`} value={item.effect} disabled={!canManage||saving===item.capability} onChange={event=>update(item,event.target.value as Grant["effect"])}><option value="inherit">Наследовать</option><option value="allow">Разрешить</option><option value="deny">Запретить</option></select></div>)}</div>}
