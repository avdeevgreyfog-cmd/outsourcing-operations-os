import { NextResponse } from "next/server";
import { z } from "zod";
import { getCurrentActor } from "@/lib/auth/server";
import { requireCapability, AccessDeniedError } from "@/lib/access/server";
import { withTenant } from "@/lib/db/client";

const schema=z.object({name:z.string().trim().min(2).max(160),legalName:z.string().trim().max(240).optional(),inn:z.string().trim().max(20).optional(),regionId:z.string().uuid().optional()});
export async function POST(request:Request){try{const actor=await getCurrentActor();if(!actor)return NextResponse.json({error:"Unauthorized"},{status:401});requireCapability(actor,"sales.client.create");if(actor.demo)return NextResponse.json({error:"Demo dataset is read-only. Connect PostgreSQL to persist changes."},{status:409});const body=schema.parse(await request.json());const row=await withTenant(actor.organizationId,actor.userId,async sql=>{const [created]=await sql<Array<{id:string;name:string;legalName:string|null;status:string}>>`INSERT INTO client_companies (organization_id,name,legal_name,inn,owner_user_id,created_by_user_id,assigned_team_id,region_id) VALUES (${actor.organizationId}::uuid,${body.name},${body.legalName??null},${body.inn??null},${actor.userId}::uuid,${actor.userId}::uuid,${actor.teamIds[0]??null}::uuid,${body.regionId??null}::uuid) RETURNING id,name,legal_name "legalName",status`;return created});return NextResponse.json(row,{status:201})}catch(error){if(error instanceof z.ZodError)return NextResponse.json({error:"Validation failed",issues:(error as z.ZodError).issues},{status:400});if(error instanceof AccessDeniedError)return NextResponse.json({error:"Forbidden"},{status:403});console.error(error);return NextResponse.json({error:"Internal error"},{status:500})}}
