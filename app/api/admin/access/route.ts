import {NextResponse} from "next/server";import {z} from "zod";import {getCurrentActor} from "@/lib/auth/server";import {AccessDeniedError,requireCapability} from "@/lib/access/server";import {withTenant} from "@/lib/db/client";
const schema=z.object({membershipId:z.string().uuid(),capability:z.string().min(3).max(160),effect:z.enum(["inherit","allow","deny"]),scopeType:z.enum(["own_created","assigned_to_me","team","region","objects","clients","all_org"]).optional(),scopeIds:z.array(z.string().uuid()).max(200).optional()});
export async function PATCH(request:Request){try{const actor=await getCurrentActor();if(!actor)return NextResponse.json({error:"Unauthorized"},{status:401});requireCapability(actor,"admin.permissions.manage");if(actor.demo)return NextResponse.json({error:"Demo is read-only"},{status:409});const body=schema.parse(await request.json());await withTenant(actor.organizationId,actor.userId,async sql=>{const [target]=await sql<Array<{id:string}>>`SELECT id FROM organization_memberships WHERE id=${body.membershipId}::uuid AND organization_id=${actor.organizationId}::uuid`;if(!target)throw new AccessDeniedError("admin.permissions.manage");const [definition]=await sql<Array<{capability:string}>>`SELECT capability FROM permission_definitions WHERE capability=${body.capability}`;if(!definition)return Promise.reject(new Error("Unknown capability"));await sql`DELETE FROM user_permission_overrides WHERE membership_id=${body.membershipId}::uuid AND capability=${body.capability}`;if(body.effect!=="inherit"){let ids=body.scopeIds??[];if(body.effect==="allow"&&body.scopeType==="region"&&ids.length===0){const regions=await sql<Array<{region_id:string}>>`SELECT region_id FROM membership_regions WHERE membership_id=${body.membershipId}::uuid`;ids=regions.map(x=>x.region_id)}await sql`INSERT INTO user_permission_overrides(organization_id,membership_id,capability,effect,scope_type,scope_ids,changed_by_user_id) VALUES(${actor.organizationId}::uuid,${body.membershipId}::uuid,${body.capability},${body.effect},${body.effect==="allow"?body.scopeType??"assigned_to_me":null},${ids}::uuid[],${actor.userId}::uuid)`}});return NextResponse.json({ok:true})}catch(error){if(error instanceof z.ZodError)return NextResponse.json({error:"Validation failed",issues:error.issues},{status:400});if(error instanceof AccessDeniedError)return NextResponse.json({error:"Forbidden"},{status:403});console.error(error);return NextResponse.json({error:"Internal error"},{status:500})}}
