import { NextResponse } from "next/server";
import { z } from "zod";
import { getCurrentActor } from "@/lib/auth/server";
import { requireCapability,AccessDeniedError } from "@/lib/access/server";
import { withTenant } from "@/lib/db/client";
const role=z.object({specialtyId:z.string().uuid(),count:z.number().int().positive().max(5000)});
const schema=z.object({clientId:z.string().uuid(),title:z.string().trim().min(3).max(240),location:z.string().trim().min(2).max(300),regionId:z.string().uuid(),startDate:z.string().date().optional(),durationText:z.string().trim().max(120).optional(),schedule:z.record(z.string(),z.json()).default({}),lunchPaid:z.boolean().default(false),vatMode:z.string().trim().max(40).default("with_vat"),housingRule:z.string().trim().max(120).optional(),travelRule:z.string().trim().max(120).optional(),shuttleRule:z.string().trim().max(120).optional(),ppeRule:z.string().trim().max(120).optional(),medicalRule:z.string().trim().max(120).optional(),citizenshipRule:z.string().trim().max(120).optional(),toolsRule:z.string().trim().max(120).optional(),comments:z.string().trim().max(3000).optional(),roles:z.array(role).min(1).max(40)});
export async function POST(request:Request){try{const actor=await getCurrentActor();if(!actor)return NextResponse.json({error:"Unauthorized"},{status:401});requireCapability(actor,"sales.request.create");if(actor.demo)return NextResponse.json({error:"Demo dataset is read-only. Connect PostgreSQL to persist changes."},{status:409});const body=schema.parse(await request.json());const result=await withTenant(actor.organizationId,actor.userId,async sql=>sql.begin(async tx=>{const [r]=await tx<any[]>`INSERT INTO requests (organization_id,client_company_id,title,status,location_text,region_id,expected_start_date,duration_text,schedule_json,lunch_paid,vat_mode,housing_rule,travel_rule,shuttle_rule,ppe_rule,medical_rule,citizenship_rule,tools_rule,comments,owner_user_id,created_by_user_id,assigned_team_id) VALUES (${actor.organizationId}::uuid,${body.clientId}::uuid,${body.title},'draft',${body.location},${body.regionId}::uuid,${body.startDate??null}::date,${body.durationText??null},${sql.json(body.schedule)},${body.lunchPaid},${body.vatMode},${body.housingRule??null},${body.travelRule??null},${body.shuttleRule??null},${body.ppeRule??null},${body.medicalRule??null},${body.citizenshipRule??null},${body.toolsRule??null},${body.comments??null},${actor.userId}::uuid,${actor.userId}::uuid,${actor.teamIds[0]??null}::uuid) RETURNING id,title,status`;for(const rr of body.roles)await tx`INSERT INTO request_roles (organization_id,request_id,specialty_id,count_required,requirements_json) VALUES (${actor.organizationId}::uuid,${r.id}::uuid,${rr.specialtyId}::uuid,${rr.count},'{}'::jsonb)`;return r}));return NextResponse.json(result,{status:201})}catch(error){if(error instanceof z.ZodError)return NextResponse.json({error:"Validation failed",issues:(error as z.ZodError).issues},{status:400});if(error instanceof AccessDeniedError)return NextResponse.json({error:"Forbidden"},{status:403});console.error(error);return NextResponse.json({error:"Internal error"},{status:500})}}
