import {requireActor} from "@/lib/auth/server";import {listWorkers} from "@/lib/data/service";import {Empty,PageHeader,Section} from "@/components/UI";
export default async function Compliance(){const actor=await requireActor();const workers=await listWorkers(actor);return <><PageHeader eyebrow="Контроль" title="Compliance" subtitle="Документы, медицина и обязательные проверки перед допуском к работе." breadcrumbs={[{label:"Контроль"},{label:"Compliance"}]}/><Section title="Проверки персонала" note={`${workers.length} сотрудников в доступном контуре`}><Empty title="Правила проверки не настроены" text="Создайте типы документов и требования к допуску в справочниках. Система не будет считать сотрудника допущенным без явных правил."/></Section></>}
