import { requireActor } from "@/lib/auth/server"; import { getCommandCenter } from "@/lib/data/service"; import { PageHeader,Section } from "@/components/UI";
export default async function Activity(){const actor=await requireActor();const data=await getCommandCenter(actor);return <><PageHeader eyebrow="Контроль" title="Activity" subtitle="Человекочитаемый поток действий дополняет технический audit. Критические изменения дополнительно фиксируют actor, before/after и timestamp."/><Section>{data.activity.map((x:any,i:number)=><div className="activity-line" key={i}><time>{x.time}</time><div><strong>{x.actor}</strong><br/><span>{x.text}</span></div></div>)}</Section></>}
