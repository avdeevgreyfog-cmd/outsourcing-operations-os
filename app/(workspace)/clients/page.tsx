import { requireActor } from "@/lib/auth/server"; import { listClients } from "@/lib/data/service"; import { PageHeader,Section,Status } from "@/components/UI"; import { CreateClientButton } from "@/components/forms/CreateClientButton";
export default async function Clients(){const actor=await requireActor();const rows=await listClients(actor);return <><PageHeader eyebrow="Коммерция" title="Клиенты" subtitle="Единый клиентский реестр. Клиент — не изолированная карточка: из него прослеживаются заявки, расчёты, объекты и activity." actions={<CreateClientButton/>}/><Section><table className="data-table"><thead><tr><th>Компания</th><th>Юр. лицо</th><th>Контакты</th><th>Заявки</th><th>Объекты</th><th>Статус</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td className="cell-title">{x.name}</td><td>{x.legalName}</td><td className="num">{x.contacts}</td><td className="num">{x.requests}</td><td className="num">{x.objects}</td><td><Status tone="good">{x.status}</Status></td></tr>)}</tbody></table></Section></>}
