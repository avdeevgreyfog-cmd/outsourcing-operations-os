import {requireActor} from "@/lib/auth/server";import {getTimesheet} from "@/lib/data/service";import {Empty,Metric,PageHeader,Section,Status} from "@/components/UI";import {TimesheetWorkspace} from "@/components/TimesheetWorkspace";import {hasCapability} from "@/lib/core/access.mjs";
export default async function Timesheets(){const actor=await requireActor();const data=await getTimesheet(actor);if(!data)return <><PageHeader eyebrow="Операции" title="Табели и сверки"/><Empty title="Нет доступного табеля" text="Scope текущего пользователя не включает доступные периоды."/></>;const sensitive=hasCapability(actor.access,"worker.compensation.read");return <><PageHeader eyebrow="Единый факт времени" title="Табели и сверка" subtitle="Клиентский и внутренний виды строятся из одного фактического слоя." breadcrumbs={[{label:"Операции"},{label:"Табели"}]}/><div className="metrics-grid"><Metric label="Период данных" value={data.period}/><Metric label="Внутренний факт" value={`${data.internalHours} ч`}/><Metric label="Подтверждено клиентом" value={`${data.clientHours} ч`}/><Metric label="Расхождение" value={`${data.discrepancy} ч`} tone={data.discrepancy?"warn":"good"}/></div><TimesheetWorkspace data={data} sensitive={sensitive}/><Section title="Сверка Internal fact ↔ Client confirmed" note="Расхождение имеет причину, владельца и статус"><div style={{padding:14}}><div className="reconcile"><div><span>Internal</span><strong>{data.internalHours} ч</strong></div><div><span>Δ</span><strong>{data.discrepancy}</strong></div><div><span>Client</span><strong>{data.clientHours} ч</strong></div><div><span>Статус</span><strong><Status tone={data.discrepancy?"warn":"good"}>{data.status}</Status></strong></div></div>{data.issue&&<div className="stack-item" style={{marginTop:12,border:"1px solid var(--border)",borderRadius:6}}><div><strong>{data.issue.worker} · {data.issue.date}</strong><small>{data.issue.reason} · ответственный {data.issue.owner}</small></div><Status tone="bad">Δ {data.issue.difference} ч</Status></div>}</div></Section></>}
