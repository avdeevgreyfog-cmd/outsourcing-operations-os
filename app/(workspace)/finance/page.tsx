import { requireActor } from "@/lib/auth/server"; import { listFinance } from "@/lib/data/service"; import { Metric,PageHeader,Section,Status } from "@/components/UI"; import { pct,rub } from "@/lib/ui/format";
export default async function Finance(){const actor=await requireActor();const rows=await listFinance(actor);const rev=rows.reduce((s,x)=>s+Number(x.revenue||0),0),cost=rows.reduce((s,x)=>s+Number(x.workerCost||0)+Number(x.expenses||0),0),cont=rows.reduce((s,x)=>s+Number(x.contribution||0),0);return <><PageHeader eyebrow="Финансы" title="Contribution P&L" subtitle="Факт выручки строится из подтверждённых биллинговых фактов и ClientRate; worker cost — из начислений. Табель не используется как единственный payout ledger."/><div className="metrics-grid"><Metric label="Выручка" value={rub(rev)}/><Metric label="Worker + object cost" value={rub(cost)}/><Metric label="Contribution" value={rub(cont)} tone={cont>=0?"good":"bad"}/><Metric label="Weighted margin" value={rev?pct(cont/rev*100):"0%"} tone={cont>0?"good":"bad"}/></div><Section><table className="data-table"><thead><tr><th>Объект</th><th>Выручка</th><th>Worker cost</th><th>Расходы объекта</th><th>Contribution</th><th>Margin</th><th>Plan</th><th>Отклонение</th></tr></thead><tbody>{rows.map((x)=>{const delta=Number(x.marginPct)-Number(x.planMarginPct??x.marginPct);return <tr key={x.id}><td className="cell-title">{x.object}</td><td className="num">{rub(x.revenue)}</td><td className="num">{rub(x.workerCost)}</td><td className="num">{rub(x.expenses)}</td><td className="num">{rub(x.contribution)}</td><td className="num">{pct(x.marginPct)}</td><td className="num">{x.planMarginPct==null?"—":pct(x.planMarginPct)}</td><td><Status tone={delta<0?"bad":"good"}>{delta>=0?"+":""}{pct(delta)}</Status></td></tr>})}</tbody></table></Section></>}
