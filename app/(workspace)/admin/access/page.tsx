import {requireActor} from "@/lib/auth/server";import {listAccessUsers,listAudit} from "@/lib/data/service";import {PageHeader,Section,Status} from "@/components/UI";import {AccessEditor} from "@/components/AccessEditor";
export default async function Access(){const actor=await requireActor();const users=await listAccessUsers(actor);const audit=actor.access.capabilities.includes("audit.read")?await listAudit(actor):[];return <><PageHeader eyebrow="Администрирование" title="Пользователи и доступ" subtitle="Шаблон роли, индивидуальное правило, область доступа и отдельные чувствительные поля." breadcrumbs={[{label:"Администрирование"},{label:"Пользователи и права"}]}/><Section title="Редактор доступа" note="Правило сохраняется сервером, проверяется в контуре организации и попадает в журнал изменений"><AccessEditor users={users} demo={actor.demo}/></Section>{audit.length>0&&<Section title="Последние высокорисковые изменения"><table className="data-table"><thead><tr><th>Время</th><th>Пользователь</th><th>Действие</th><th>Запись</th><th>Комментарий</th></tr></thead><tbody>{audit.map(x=><tr key={x.id}><td>{x.createdAt}</td><td className="cell-title">{x.actor}</td><td><Status tone="info">{x.action}</Status></td><td>{x.record}</td><td>{x.summary??"—"}</td></tr>)}</tbody></table></Section>}</>}
