import { requireActor } from "@/lib/auth/server"; import { listCandidates } from "@/lib/data/service"; import { PageHeader,Section } from "@/components/UI"; import { CandidatesGrid } from "@/components/RegistryGrids";
export default async function Candidates(){const actor=await requireActor();const rows=await listCandidates(actor);return <><PageHeader eyebrow="Подбор" title="Кандидаты" subtitle="Единый реестр людей и их заявок на конкретные потребности." breadcrumbs={[{label:"Люди"},{label:"Подбор",href:"/recruiting"},{label:"Кандидаты"}]}/><Section><CandidatesGrid rows={rows}/></Section></>}
