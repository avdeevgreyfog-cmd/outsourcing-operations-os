import Link from "next/link";import {notFound} from "next/navigation";import {requireActor} from "@/lib/auth/server";import {listCandidates} from "@/lib/data/service";import {KeyValue,PageHeader,Section,Status} from "@/components/UI";
export default async function CandidatePage({params}:{params:Promise<{id:string}>}){const {id}=await params;const actor=await requireActor();const rows=await listCandidates(actor);const candidate=rows.find(x=>x.id===id);if(!candidate)notFound();return <><PageHeader eyebrow="Кандидат" title={candidate.fullName} subtitle={`${candidate.need??"Специальность не указана"} · ${candidate.object??"Объект не выбран"}`} breadcrumbs={[{label:"Люди"},{label:"Подбор",href:"/recruiting"},{label:"Кандидаты",href:"/candidates"},{label:candidate.fullName}]}/><div className="workspace-grid"><div><Section title="Основное"><div style={{padding:"6px 15px 14px"}}><KeyValue label="Телефон" value={candidate.phone??"—"}/><KeyValue label="Источник" value={candidate.source??"—"}/><KeyValue label="Этап" value={<Status tone="info">{candidate.stageLabel??candidate.stage}</Status>}/><KeyValue label="Следующее действие" value={candidate.nextAction??"Не запланировано"}/></div></Section><Section title="Заявка на потребность"><div style={{padding:"6px 15px 14px"}}><KeyValue label="Специальность" value={candidate.need??"—"}/><KeyValue label="Объект" value={candidate.object?<Link href={`/objects/${candidate.objectId}?tab=recruiting`}>{candidate.object}</Link>:"—"}/></div></Section></div><div><Section title="Проверки перед выходом"><div className="stack-list"><div className="stack-item"><div><strong>Контакт</strong><small>Телефон подтверждён в карточке</small></div><Status tone={candidate.phone?"good":"warn"}>{candidate.phone?"Есть":"Нет"}</Status></div><div className="stack-item"><div><strong>Первый выход</strong><small>Готовность к назначению на смену</small></div><Status tone={candidate.stage==="first_shift"?"good":"neutral"}>{candidate.stage==="first_shift"?"Подтверждён":"Не подтверждён"}</Status></div></div></Section></div></div></>}
