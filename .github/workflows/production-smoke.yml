name: Production Smoke

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  production-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      BASE_URL: https://outsourcing-operations-os-g942.vercel.app
    steps:
      - name: Wait for deployed runtime
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 36); do
            code="$(curl -sS -o /tmp/status.json -w '%{http_code}' "$BASE_URL/api/system/status" || true)"
            deployed_sha="$(node -e "try{const s=require('/tmp/status.json');process.stdout.write(s.deploymentSha||'')}catch{}" 2>/dev/null || true)"
            if [ "$code" = "200" ] && grep -q '"ok":true' /tmp/status.json && [ "$deployed_sha" = "$GITHUB_SHA" ]; then
              cat /tmp/status.json
              exit 0
            fi
            echo "attempt=$attempt status=$code deployed_sha=$deployed_sha expected_sha=$GITHUB_SHA"
            sleep 10
          done
          echo "Current commit did not become active in production in time."
          cat /tmp/status.json 2>/dev/null || true
          exit 1

      - name: Verify database-backed workspace is enabled
        shell: bash
        run: |
          set -euo pipefail
          cat /tmp/status.json
          node - <<'NODE'
          const fs = require("node:fs");
          const status = JSON.parse(fs.readFileSync("/tmp/status.json", "utf8"));
          if (status.ok !== true) throw new Error("runtime status is not healthy");
          if (status.databaseConfigured !== true) throw new Error("DATABASE_URL is not configured in production");
          if (status.demoAvailable !== true) throw new Error("demo workspace is expected to remain available");
          NODE

      - name: Verify dedicated workspace entry links
        shell: bash
        run: |
          set -euo pipefail
          demo_headers="$(mktemp)"
          work_headers="$(mktemp)"
          curl -sS -D "$demo_headers" -o /dev/null "$BASE_URL/demo"
          curl -sS -D "$work_headers" -o /dev/null "$BASE_URL/work"
          grep -qi '^location: /' "$demo_headers"
          grep -qi 'oo_workspace_mode=demo' "$demo_headers"
          grep -qi '^location: /login?force=work' "$work_headers"
          grep -qi 'oo_workspace_mode=' "$work_headers"

      - name: Verify login surface
        shell: bash
        run: |
          set -euo pipefail
          code="$(curl -sS -L -o /tmp/login.html -w '%{http_code}' "$BASE_URL/login")"
          test "$code" = "200"
          grep -q 'sergey-work' /tmp/login.html
          grep -q 'Демо-организация' /tmp/login.html

      - name: Verify control detail routes
        shell: bash
        run: |
          set -euo pipefail
          cookie='oo_workspace_mode=demo; oo_demo_role=director'
          client_code="$(curl -sS -L -H "Cookie: $cookie" -o /tmp/client.html -w '%{http_code}' "$BASE_URL/clients/70000000-0000-4000-8000-000000000001")"
          candidate_code="$(curl -sS -L -H "Cookie: $cookie" -o /tmp/candidate.html -w '%{http_code}' "$BASE_URL/candidates/85000000-0000-4000-8000-000000000001")"
          request_code="$(curl -sS -L -H "Cookie: $cookie" -o /tmp/request.html -w '%{http_code}' "$BASE_URL/requests/73000000-0000-4000-8000-000000000001")"
          echo "client_status=$client_code candidate_status=$candidate_code request_status=$request_code"
          test "$client_code" = "200"
          test "$candidate_code" = "200"
          test "$request_code" = "200"

      - name: Verify object and worker detail routes
        shell: bash
        run: |
          set -euo pipefail
          cookie='oo_workspace_mode=demo; oo_demo_role=director'
          object_path='/objects/80000000-0000-4000-8000-000000000001'
          worker_path='/workers/88000000-0000-4000-8000-000000000001'
          object_code="$(curl -sS -L -H "Cookie: $cookie" -o /tmp/object.html -w '%{http_code}' "$BASE_URL$object_path")"
          worker_code="$(curl -sS -L -H "Cookie: $cookie" -o /tmp/worker.html -w '%{http_code}' "$BASE_URL$worker_path")"
          echo "object_status=$object_code"
          echo "worker_status=$worker_code"
          if [ "$object_code" != "200" ]; then head -c 3000 /tmp/object.html || true; echo; exit 1; fi
          if [ "$worker_code" != "200" ]; then head -c 3000 /tmp/worker.html || true; echo; exit 1; fi
          if grep -q "This page couldn" /tmp/object.html; then head -c 3000 /tmp/object.html; echo; exit 1; fi
          if grep -q "This page couldn" /tmp/worker.html; then head -c 3000 /tmp/worker.html; echo; exit 1; fi
